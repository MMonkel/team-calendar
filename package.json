{
  "name": "azure-webapp-starter",
  "private": true,
  "workspaces": [
    "shared",
    "api",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=api & npm run dev --workspace=web",
    "build": "npm run build --workspace=shared && npm run build --workspace=web && npm run build --workspace=api",
    "typecheck": "npm run build --workspace=shared && npm run typecheck --workspace=api && npm run typecheck --workspace=web"
  },
  "engines": {
    "node": ">=22"
  }
}
